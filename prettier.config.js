//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  tabWidth: 2,
  semi: true,
  bracketSpacing: true,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 120,
};

export default config;
